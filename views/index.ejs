<section class="center-wrap">

  <div class="glass search-card">
    <h1 class="title">Search Weather</h1>
    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <form id="searchForm" action="/weather" method="POST" class="search-form">
      <input id="cityInput" name="city" type="text" placeholder="City name or 'lat,lon' (auto-detect with 📍)" autocomplete="off" required />
      <div class="row">
        <button type="submit" class="btn">Search</button>
        <button type="button" id="micBtn" class="btn icon">🎤</button>
        <button type="button" id="gpsBtn" class="btn icon">📍</button>
      </div>
    </form>

    <div class="note">Tip: You can say "Mumbai" or click 📍 to use current location. Last searched city is saved locally.</div>
  </div>

  <div class="footer-note">Built with WeatherAPI • Enter city name, postal code or "lat,lon"</div>

</section>
